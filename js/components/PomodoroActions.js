import React from 'react';

let PomodoroActions = React.createClass({

  render() {
    return(
      <div>
        <button>Start</button>
        <button>Reset</button>
      </div>
    );
  }
});

export default PomodoroActions;
